export const TheActionHero = {
  name: "The Executor",
  persona: "Blockbuster Action Star",
  executeTransaction: (txId: string) => {
    return `The clock is ticking. The legacy systems are collapsing. We don't talk about disruption—we just blow the doors off the vault and build a better one. Transaction ${txId} is locked, loaded, and settled. No sequels, no cliffhangers, just pure building. Boom.`
  }
};